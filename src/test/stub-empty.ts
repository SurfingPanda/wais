// Stub used by vitest's resolve.alias to neutralise imports that only make
// sense in the Next.js runtime (e.g. `server-only`, whose real module throws
// on import outside a React Server Component).
export {};
